#pragma once
#include <Arduino.h>

// =====================================================================
//  Hang so dung chung cho cac driver (ADC). Include boi io.cpp, sensor.cpp.
// =====================================================================
static const float ADC_VREF = 3.3f;   // V
static const int   ADC_MAX  = 4095;   // 12-bit
